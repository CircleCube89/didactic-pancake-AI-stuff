# Итоговый проект по курсу «Инженерия Искусственного Интеллекта»

В этой папке находится итоговый мини-проект по курсу.  
Проект должен демонстрировать применение методов и инструментов инженерии ИИ: работу с данными, модели, пайплайны, сервис, эксперименты и (по возможности) воспроизводимость.

---

## 1. Краткое описание:

   Проект посвящён построению сервиса, который на основе информации о студенте и его подготовке пресказывает его оценку на экзамене.  

---

## 2. Структура проекта

Проект организован в следующей структуре:

- `requirements.txt` – зависимости проекта (библиотеки Python, необходимые для запуска).
- `report.md` – отчёт по проекту (постановка задачи, данные, эксперименты, результаты).
- `self-checklist.md` – чеклист самопроверки проекта перед сдачей.
- `notebooks/` – экспериментальные ноутбуки:
  - EDA, прототипы моделей, отдельные эксперименты.
- `src/` – основной код проекта:
  - модули для работы с данными;
  - запуск сервиса.
- `data/` – учебные данные
- `artifacts/` – сохранённые модели.

Если структура проекта отличается от базовой, опишите это здесь.

---

## 3. Требования и установка

### 3.1. Требования

- Python `>= 3.10`.
- Windows 10/11.


### 3.2. Установка окружения

Примерный сценарий (можно адаптировать под ваш проект):

```bash
# Перейти в папку проекта
cd project

# Создать виртуальное окружение
python -m venv .venv

# Активировать окружение:
# Windows:
.venv\Scripts\activate

# Установить зависимости
pip install --upgrade pip
pip install -r requirements.txt
python configure.py
```

---

## 4. Как запустить проект

```bash
cd project
.venv\Scripts\activate
cd src
uvicorn api:app --reload           
```

- Сервис запускается на http://127.0.0.1:8000;
- Реализованы `/predict` и  `/health`.
- На `/docs` работает Swagger UI для отладки. `data/service_validate_*.json` - данные для проверки сервиса.

---

## 5. Данные

- Используется открытый датасет [(ссылка на источник);](https://www.kaggle.com/datasets/itszubi/students-academic-performance-dataset), копия находится  в `data/StudentPerformanceFactors.csv`.

---

## 6. Демонстрация на защите

1. Кратко покажу структуру проекта (`notebooks/`, `src/`, `data/`).
2. Запущу сервис по инструкции выше, покажу пару запросов через Swagger UI.
3. Покажу ноутбук с основными экспериментами и сравнение моделей по метрике качества.

---

## 8. Ограничения и дальнейшая работа

Кратко опишите:

- Линейная регрессия слишком хорошо предсказывает ответ, возможно данные синтетические (явно это нигде не указанно).

- Оптимизация проекта: уменьшение числа зависимостей  и общего размера сервиса.

---