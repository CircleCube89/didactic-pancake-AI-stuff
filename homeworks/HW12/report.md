# HW12 – временные ряды: temporal split, baseline-модели и GRU-прогноз

## 1. Кратко: что сделано

- S12-hw-dataset.csv
- Был организован `train / validation / test` с учетом времени
- Какие подходы сравнивались (`B1`, `B2`, `B3`, `R1`).
- Лучший - B3

## 2. Среда и воспроизводимость

- Python:3.9
- pandas / numpy / scikit-learn:
- torch:
- Устройство (CPU/GPU):GPU
- Seed:42
- Как запустить: открыть `HW12.ipynb` и выполнить Run All.

## 3. Данные и постановка задачи

- Датасет: `S12-hw-dataset.csv`
- Целевая колонка: `target`
- Частота временного ряда:
- Горизонт прогноза:1
- Размер датасета: (4320,2)
- Диапазон дат:2025-01-01 00:00:00 - 2025-06-29 23:00:00


## 4. Temporal split и признаки

### 4.1. Разбиение по времени

- Как именно выполнен `train / validation / test`:
- Какие доли или интервалы использовались:train_frac = 0.70; val_frac = 0.15; test_frac = 0.15
- Почему `random split` здесь некорректен: теряется зависимость от времени

### 4.2. Признаки для baseline-моделей

- Какие лаги использовались: "lag_1"
- Какие rolling-признаки использовались:
- Какие календарные признаки использовались:
- Как обрабатывались пропуски после построения лагов/rolling:
- Использовалось ли масштабирование и где именно оно обучалось:

## 5. Модели и эксперименты (B1, B2, B3, R1)

Опишите коротко и сопоставимо:

- B1 (`naive-last`):прогноз на следующий шаг равен последнему наблюдённому значению.
- B2 (`moving-average`):прогноз берётся как среднее нескольких последних наблюдений.
- B3 (`ridge-lag-features`):линейная модель с L2-регуляризацией.
- R1 (`gru-forecast`): модель для временных рядов.

## 6. Результаты

Ссылки на файлы в репозитории:

- Таблица результатов: `./artifacts/runs.csv`
- Лучшая `GRU`: `./artifacts/best_gru.pt`
- Конфиг лучшей `GRU`: `./artifacts/best_gru_config.json`
- Визуализация split: `./artifacts/figures/series_split.png`
- Сравнение подходов: `./artifacts/figures/baselines_compare.png`
- Кривые обучения `GRU`: `./artifacts/figures/gru_learning_curves.png`
- Финальный прогноз на test: `./artifacts/figures/best_forecast_test.png`

Короткая сводка (6-10 строк):

- Лучший baseline среди `B1`, `B2`, `B3`:
- Лучшая `val_MAE`:
- Лучшая `val_RMSE`:
- Лучшая `val_MAPE`:
- Итоговая `test_MAE` лучшего подхода:
- Итоговая `test_RMSE` лучшего подхода:
- Итоговая `test_MAPE` лучшего подхода:
- Что дали лаговые и rolling-признаки относительно простых baseline:
- Что показала `GRU` относительно табличного baseline:
- Совпали ли выводы по `validation` и `test`: да

## 7. Анализ

(8-15 предложений)

Нужно прокомментировать:

- почему наивный baseline оказался слабым или, наоборот, surprisingly сильным; из за небольшого горизонта и большого числа данных.
- что дали `moving average` и лаговые признаки; сглаженность предсказаний.
- почему `Ridge` на признаках сработала лучше или хуже простых baseline;
- почему `GRU` помогла или не помогла на данном ряде; недостаточно параметров у модели.
- насколько заметны тренд/сезонность и как это отражается на прогнозе;
- где в такой задаче могли бы появиться утечки данных; при появлении окон наблюдения.
- почему выбор по `validation`, а не по `test`, принципиален; test дальше от `train` , ссоответственно более независим.
- какие ошибки модели оказались наиболее показательными. 

## 8. Итоговый вывод

(3-7 предложений)

- Какой подход вы бы взяли как базовый для такой задачи и почему.
Любая модель, чтобы можно было увеличить горизонт.
- В каком случае достаточно простого baseline на признаках. При периодическом характере входных данных. 
- В каком случае имеет смысл переходить к `GRU`/другим нейросетям. Когда имеется сложная/непериодическая зависимость от времени.
- Что главное вы поняли про корректную валидацию временных рядов. Нельзя использовать `random split`

## 9. Приложение (опционально)

не выполнялось.
