# HW14 – эмбеддинги, FAISS, оценка retrieval и mini-RAG по базе знаний

## 1. Кратко: что сделано

- Какая база знаний использовалась и почему. - Несколько статей с habr.ru


## 2. Среда и воспроизводимость

- Python: 3.9
- faiss / faiss-cpu: faiss-cpu
- sentence-transformers / transformers / sklearn: sentence-transformers
- torch (если использовался): нет
- Устройство (CPU/GPU): cpu
- Seed:42
- Как запустить: открыть `HW14.ipynb` и выполнить Run All.

## 3. База знаний и подготовка документов

- Предметная область: SDR
- Источник документов: habr.ru
- Число исходных документов: 4
- Число чанков после разбиения: 513
- Какой способ чанкинга использовался: по словам
- Основные параметры чанкинга: chunk_size=18, overlap=5


## 4. Retrieval и индекс

### 4.1. Эмбеддинги и индекс

- Какая embedding-модель или способ векторизации использовались:SentenceTransformer 
- Какая метрика сходства использовалась:VectorSearchIndex
- Какой индекс `FAISS` использовался:384
- Какой `top_k` использовался по умолчанию:5
- Что показали первые примеры retrieval:

### 4.2. Контрольные запросы и оценка retrieval

- Сколько контрольных запросов использовалось: 6 
- Что считалось релевантным ответом:
- Какие метрики retrieval считались:
- Итоговый `hit@k`:0.666667
- Итоговый `recall@k`:0.666667
- Дополнительные метрики (если были): mean_MRR@3: 0.666667

- Какие запросы оказались самыми простыми:
- Какие запросы оказались самыми проблемными: запросы без прямого отета в базе знаний.

## 5. Эксперимент по параметрам retrieval и обновление базы знаний

Опишите коротко и по делу:

- Какой параметр сравнивался (`chunk_size`, `overlap`, `top_k` или другой разумный вариант):
- Какие два варианта сравнивались:
- Как изменилось качество retrieval:
- Какой вариант вы выбрали как основной и почему: любой
- Что именно было обновлено в базе знаний: добавлены статьи
- Как была выполнена переиндексация: дополнение старых и еоых к уже существующим.
- Что изменилось в retrieval после обновления базы знаний: ничего

## 6. Mini-RAG и результаты

Ссылки на файлы в репозитории:

- Оценка retrieval: `./artifacts/retrieval_eval.csv`
- Примеры ответов mini-RAG: `./artifacts/rag_examples.csv`
- Сравнение до/после обновления: `./artifacts/retrieval_before_after_update.csv`

Короткая сводка (6-10 строк):

- Как формировался контекст для ответа: k лучших
- Какой способ генерации ответа использовался:
- Возвращались ли источники вместе с ответом: да
- На каких вопросах mini-RAG сработал лучше всего:
- На каких вопросах mini-RAG ошибался или отвечал неуверенно:
- Какую роль сыграло качество retrieval в итоговом ответе:

## 7. Анализ

(8-15 предложений)

Нужно прокомментировать:

- насколько удачной оказалась выбранная база знаний- неудачно, тк данный метод требует прямого ответа на вопрос в базе данных.
- как выбор чанкинга повлиял на retrieval; - при меньших значениях точность выше.
- насколько полезной оказалась формальная оценка retrieval;
- что показал эксперимент с параметрами;
- как обновление базы знаний повлияло на результаты; - не изменились



## 8. Итоговый вывод

(3-7 предложений)

из за низкой информаивности базы пострадало качество ответов

## 9. Приложение (опционально)

не выполнялось